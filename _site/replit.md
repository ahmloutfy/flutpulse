# FlutPulse on Replit

## Run locally

Install the Ruby dependencies with:

```bash
bundle install
```

Start the Jekyll site with:

```bash
bundle exec jekyll serve --host 0.0.0.0 --port 5000
```

The Replit workflow runs this command automatically on port 5000.